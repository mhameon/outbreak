{
  "name": "outbreak-server",
  "version": "1.0.0",
  "description": "Zombies experiments",
  "main": "src/index.ts",
  "scripts": {
    "start": "NODE_CONFIG_DIR=../config node build",
    "clean": "npx rimraf build",
    "build": "npm run clean && echo 'Compiling Typescript' && npx tsc --version && npx tsc --pretty",
    "watch:server": "npx tsc-watch --onSuccess 'npm start'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build"
  },
  "author": "Matt",
  "devDependencies": {
    "@types/config": "0.0.36",
    "@types/express": "^4.17.6",
    "@types/mocha": "^7.0.2",
    "@types/socket.io": "^2.1.4"
  },
  "dependencies": {
    "config": "^3.3.1",
    "express": "^4.17.1",
    "mocha": "^7.1.1",
    "module-alias": "^2.2.2",
    "socket.io": "^2.3.0",
    "spdy": "^4.0.2",
    "tsc-watch": "^4.2.3",
    "typescript": "^3.8.3",
    "winston": "^3.2.1"
  },
  "_moduleAliases": {
    "@service": "build/service"
  }
}
